apiVersion: v1
kind: Secret
metadata:
  name: cloudsql-db-credentials
  namespace: wp-{{ .Values.name }}
  labels:
    app: wp-{{ .Values.name }}
type: Opaque
data:
  username: |-
    {{ .Files.Get "files/.dbuser" | b64enc }}
  password: |-
    {{ .Files.Get "files/.dbpw" | b64enc }}
